# Frontend API base URL. Leave empty when using the Vite proxy in local dev.
VITE_API_URL=

# Optional browser key for Google Maps Embed API.
# Restrict this key by HTTP referrer and only enable Maps Embed API.
VITE_GOOGLE_MAPS_EMBED_API_KEY=
